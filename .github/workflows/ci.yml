name: CI/CD Pipeline

on:
  push:
    branches:
      - master      # Le pipeline se déclenche à chaque push sur la branche master

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Vérification du déclenchement
        run: echo "Trigger fonctionne "
